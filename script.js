document.getElementById("criarConta").addEventListener("click", function(e) {
    e.preventDefault(); // evita o comportamento padrão do link
    window.location.href = "cadastro.html"; // vai para outra página
});